# Python
backend/.venv/
__pycache__/
*.py[cod]
*.pyo

# Node
frontend/node_modules/
frontend/dist/

# Logs
/tmp/generative-radio-*.log

# macOS
.DS_Store

# Editor
.cursor/
*.swp

# User accounts database (created at runtime by backend/users.py).
# The only stateful file in the repo besides the library volume — back it up.
backend/users.db
backend/users.db-wal
backend/users.db-shm

# Local secrets override (JWT_SECRET / INVITE_CODE normally live in
# ~/.generative-radio.env, outside the repo)
.env

# Saved tracks
saved_tracks/

# User reaction records (generated at runtime)
tracks_with_user_action/
node_modules
